var React = require('react-with-addons');

var DataDialog = React.createClass({

  render: function() {

    return (
      <div>
        Data Dialog Content
      </div>
    );
  }
});

module.exports = DataDialog;